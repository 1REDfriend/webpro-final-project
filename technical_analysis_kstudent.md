# รายงานวิเคราะห์ทางเทคนิค: ระบบ KStudent School Management System
> จัดทำโดย: Technical Lead & Systems Analyst  
> วันที่: 28 กุมภาพันธ์ 2569

---

## 1. ภาพรวมโปรเจกต์ (Executive Summary)

- **ชื่อระบบ**: KStudent School Management System
- **วัตถุประสงค์หลัก**: ระบบบริหารจัดการโรงเรียนแบบครบวงจร ผ่านเว็บแอปพลิเคชัน รองรับผู้ใช้งาน 5 บทบาท (Role) ได้แก่ นักเรียน (Student), ครู (Teacher), เจ้าหน้าที่ (Staff), ผู้บริหาร (Executive) และผู้จัดการ (Manager)
- **ลักษณะการทำงาน**: ระบบ Multi-Role Web Application แบบ Server-Side Rendering (SSR) ที่ให้แต่ละบทบาทเข้าถึงข้อมูลและฟังก์ชันที่เหมาะสมผ่าน URL ที่แยกจากกัน
- **ที่เก็บโค้ด (Repository)**: [github.com/1REDfriend/webpro-final-project](https://github.com/1REDfriend/webpro-final-project)

---

## 2. เทคโนโลยีและเครื่องมือที่ใช้ (Technology Stack)

### Frontend
- **Template Engine**: EJS (Embedded JavaScript) v4.0.1 — ใช้สร้าง HTML ฝั่ง Server
- **Styling**: Vanilla CSS (ไม่ใช้ Framework)
- **ไลบรารีภายนอก (CDN)**: SweetAlert2 (สำหรับ Dialog Confirmations)
- **ไลบรารีภายนอก (CDN)**: เข้าใจว่ามีการใช้ JavaScript ฝั่ง Client สำหรับ Live Search filter

### Backend
- **Runtime**: Node.js (CommonJS Modules)
- **Framework**: Express.js v5.2.1
- **รูปแบบ API**: Server-Side Rendering (SSR) — ไม่ใช่ REST API, ข้อมูลส่งผ่าน Form POST / GET และ Redirect
- **Session Management**: express-session v1.19.0
- **File Upload**: Multer v2.0.2
- **PDF Generation**: Puppeteer v24.37.5
- **CSV Processing**: csv-parse v6.1.0 + csv-stringify v6.6.0

### Database & Infrastructure
- **ฐานข้อมูลหลัก**: SQLite3 v5.1.7 — เก็บเป็นไฟล์ [kstudent.sqlite](file:///d:/work/write_code/webpro-final-project/kstudent.sqlite) ในโลคัล
- **Dependency ที่ติดตั้งแต่ไม่ได้ใช้จริง**: mysql2 v3.16.3 (ปรากฏใน [package.json](file:///d:/work/write_code/webpro-final-project/package.json) แต่ไม่มีการ require ในโค้ด)
- **การรัน Server**: Script [run-server.bat](file:///d:/work/write_code/webpro-final-project/run-server.bat) สำหรับ Windows (ตรวจสอบ Node.js, ติดตั้ง Dependency, Seed DB และรัน Nodemon)
- **Hosting**: Local Machine (Port 3000) — ไม่มี Cloud Deployment Configuration ในโปรเจกต์

---

## 3. โครงสร้างสถาปัตยกรรมระบบ (System Architecture)

### รูปแบบโครงสร้าง: MVC (Model-View-Controller)

- **Model**: [database.js](file:///d:/work/write_code/webpro-final-project/database.js) — กำหนด Schema และ SQLite Connection โดยตรง (ไม่ใช้ ORM)
- **View**: ไดเรกทอรี `views/` — ไฟล์ EJS แยกตาม Role
- **Controller**: ไดเรกทอรี `src/controllers/` — ไฟล์ JS แยกตาม Role
- **Router**: ไดเรกทอรี `src/routes/` — ไฟล์ JS แยกตาม Role
- **Middleware**: `src/middleware/authMiddleware.js` — ตรวจสอบสิทธิ์ก่อนเข้าถึง Route

### การแบ่งส่วนของโมดูล (Module Breakdown)

| Module | Route Prefix | Controller | Views (จำนวน) |
|---|---|---|---|
| Authentication | `/` | _(ใน authRoutes)_ | `auth/login.ejs` (1) |
| Student | `/student` | `studentController.js` | 8 ไฟล์ |
| Teacher | `/teacher` | `teacherController.js` | 7 ไฟล์ |
| Staff | `/staff` | `staffController.js` | 2 ไฟล์ |
| Executive | `/executive` | `executiveController.js` | 6 ไฟล์ |
| Manager | `/sys-admin` | `managerController.js` | 1 ไฟล์ |

> [!NOTE]
> Route `/sys-admin` ถูก Map ไปยัง Role `manager` ใน Code แต่ใน `users` Table ค่า `role` ที่ใช้จริงคือ `executive` (ไม่มี `manager` ใน CHECK constraint) — อาจเป็นจุดที่ต้องตรวจสอบ

---

## 4. ฟีเจอร์และระบบการทำงานหลัก (Core Features)

### 🔐 ระบบยืนยันตัวตน (Authentication)
- Login ด้วย Username/Password และ Role-Based Access Control (RBAC)
- ใช้ `express-session` บันทึก Session ฝั่ง Server
- Middleware ปกป้อง Route ตาม Role

### 👨‍🎓 บทบาทนักเรียน (Student)
- ดูแดชบอร์ดส่วนตัว (รูปโปรไฟล์, ห้องเรียน, ประกาศโรงเรียน)
- ดูผลการเรียน (Grades) พร้อมคำนวณ GPA แบบ Real-time จากโค้ด
- ดูตารางสอน (Schedule)
- ดูและส่งคำร้อง (Requests) พร้อมแนบไฟล์, ยกเลิกคำร้องได้ภายใน 1 ชั่วโมง
- ดูประวัติคะแนนความประพฤติ (Behavior History)
- ดาวน์โหลดใบแสดงผลการเรียน (Transcript) เป็นไฟล์ **PDF** (ผ่าน Puppeteer)
- ดูประกาศโรงเรียน (Announcements) ทั้งหมดและแบบรายการ

### 👩‍🏫 บทบาทครู (Teacher)
- จัดการรายวิชา (Subjects) ที่ตนรับผิดชอบ
- บันทึก/แก้ไขเกรดนักเรียนรายคน
- **อัปโหลดเกรดจาก CSV** และดาวน์โหลด Template CSV
- ปรับคะแนนความประพฤตินักเรียน พร้อมบันทึก Log
- ดูตารางสอนของตนเองและห้องที่ดูแล (Homeroom)
- เลือกห้องประจำชั้น (Homeroom) ที่ตนดูแล
- ส่งและจัดการคำร้อง
- ดูข้อมูลเกรดของนักเรียนแต่ละคน

### 📋 บทบาทเจ้าหน้าที่ (Staff)
- ดูคำร้องทั้งหมดจากนักเรียนและครู
- อนุมัติ/ปฏิเสธคำร้อง พร้อมตอบกลับ
- สร้าง/แก้ไข/ลบประกาศโรงเรียน (Announcements) พร้อมรูปภาพ และกำหนด Target Audience

### 📊 บทบาทผู้บริหาร (Executive)
- แดชบอร์ดภาพรวมสถิติ: จำนวนนักเรียน, ครู, เจ้าหน้าที่, GPA เฉลี่ยทั้งโรงเรียน และ GPA แยกห้องเรียน
- ดูสถานะคำร้องทั้งหมด (Pending/Approved/Rejected/Total)
- บริหารจัดการข้อมูลครู: เพิ่ม/ลบ/แก้ไข, มอบหมายวิชาและห้องประจำชั้น
- บริหารจัดการข้อมูลนักเรียน: เพิ่ม/ลบ/แก้ไข, เปลี่ยนห้องเรียน, เพิ่ม/ลบวิชาลงทะเบียน
- อัปโหลดรูปโปรไฟล์สำหรับครูและนักเรียน

### 🗂️ บทบาทผู้จัดการ (Manager)
- แดชบอร์ดสรุปสถิติคำร้อง (แยก Pending/Approved/Rejected/Total) พร้อมตาราง Request History

---

## 5. โครงสร้างข้อมูลเบื้องต้น (Data Models & Schemas)

ระบบมี **8 ตาราง** ในฐานข้อมูล SQLite:

| ตาราง | คำอธิบาย | ฟิลด์สำคัญ |
|---|---|---|
| `users` | บัญชีผู้ใช้ทุกบทบาท | `id`, `username`, `password` (Plain Text), `role`, `full_name`, `profile_pic`, `gender` |
| `students` | ข้อมูลเพิ่มเติมของนักเรียน | `user_id` (FK), `classroom_id` (FK), `student_code`, `behavior_score` (Default 100) |
| `classrooms` | ข้อมูลห้องเรียน | `id`, `name`, `homeroom_teacher_id` |
| `homeroom_teachers` | ความสัมพันธ์ Many-to-Many ครู-ห้อง | `teacher_id` (FK), `classroom_id` (FK) |
| `subjects` | รายวิชา | `id`, `code`, `name`, `credit`, `teacher_id` (FK) |
| `enrollments` | การลงทะเบียนเรียนและเกรด | `student_id` (FK), `subject_id` (FK), `grade_midterm`, `grade_final`, `total_score`, `grade_char`, `academic_year`, `semester` |
| `grade_logs` | Log การเปลี่ยนแปลงเกรด | `student_id`, `subject_id`, `action`, `old_grade_*`, `new_grade_*`, `recorded_by` |
| `behavior_logs` | Log การปรับคะแนนความประพฤติ | `student_id`, `score_change`, `reason`, `recorded_by` |
| `requests` | คำร้องจากนักเรียน/ครู | `user_id` (FK), `topic`, `description`, `attachment_url`, `status` (Pending/Approved/Rejected), `reply` |
| `announcements` | ประกาศโรงเรียน | `title`, `description`, `image_url`, `target_audience` (student/teacher/both), `created_by` |
| `schedules` | ตารางสอน | `classroom_id` (FK), `subject_id` (FK), `day`, `time_slot` |

---

## 6. ข้อสังเกตและจุดที่ควรปรับปรุง (Technical Observations)

### ⚠️ ความเสี่ยงด้านความปลอดภัย (Security Risks) — ความสำคัญสูง

- **🔴 รหัสผ่านไม่ถูก Hash**: คอลัมน์ `password` ใน `users` เก็บเป็น Plain Text ทั้งในโค้ด `addTeacher`, `addStudent` และ `seed.js` ควรใช้ `bcrypt` เพิ่มเข้าไปก่อน Production
- **🔴 ไม่มีการป้องกัน SQL Injection อย่างสมบูรณ์**: แม้ใช้ Parameterized Query เป็นส่วนใหญ่ แต่พบการคำนวณ Placeholder แบบ Dynamic (`WHERE student_id IN (${placeholders})`) ใน `teacherController.js` ควรตรวจสอบจุดดังกล่าว
- **🟡 Session Secret แบบ Hardcode**: `secret: 'kstudent-secret-key-2024'` ถูกฝังใน `server.js` ควรย้ายไปใช้ Environment Variable (`.env` file)

### 🔧 Technical Debt & Code Quality

- **🟡 Logic ซ้ำซ้อน (Code Duplication)**: ฟังก์ชัน `processGrades()` ที่คำนวณ GPA ปรากฏซ้ำอย่างน้อย 2 ครั้งใน `studentController.js` และ `teacherController.js` ควร Refactor ออกมาเป็น Shared Utility Module
- **🟡 Callback Hell**: Controller Files ใช้ Nested Callback ลึกหลายชั้น (โดยเฉพาะ `executiveController.js`) ทำให้อ่านและ Debug ยาก ควรพิจารณา Refactor ด้วย `async/await` + `Promise` ให้สม่ำเสมอ
- **🟡 Column ใน DB ไม่สอดคล้อง**: `staffController.js` พยายาม INSERT `description_markdown` ซึ่งไม่ได้ถูก Define ใน Schema ของ `database.js` บ่งชี้ถึงการ Migrate Schema ที่ไม่สมบูรณ์
- **🟡 Dependency ที่ไม่ได้ใช้**: `mysql2` ถูกติดตั้งใน `package.json` แต่ไม่มีการใช้งานจริง ควรลบออกเพื่อลด Surface Area

### 📦 โครงสร้างและการบำรุงรักษา

- **🟢 ไม่มี Test Suite**: ไม่มี Unit Test หรือ Integration Test (`package.json` มีแค่ Placeholder) ทำให้ความเสี่ยงจากการ Regression สูง
- **🟢 ไม่มี `.env` Configuration**: ค่า Config เช่น Port, Session Secret ถูก Hardcode ในโค้ด ควรใช้ไลบรารี `dotenv`
- **🟢 ฐานข้อมูล SQLite**: เหมาะสำหรับ Development/Prototype แต่ไม่แนะนำสำหรับ Production ที่มีผู้ใช้งานหลายคนพร้อมกัน ควรพิจารณา Migration ไปยัง PostgreSQL หรือ MySQL หาก Scale ขึ้น

---
*เอกสารนี้จัดทำจากการวิเคราะห์โค้ดจริงในโปรเจกต์เท่านั้น*
